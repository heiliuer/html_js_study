// def find_by_binary_search(array=[], data=0):
// length, low = len(array), 0
// high = length - 1
// while low <= high:
// mid = int((low + high) / 2)
// if data == array[mid]:
// return mid;
// elif data > array[mid]:
// low = mid + 1
// else:
// high = mid - 1
// return -1
